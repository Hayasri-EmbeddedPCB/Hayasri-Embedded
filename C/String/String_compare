#include <stdio.h>
#include <string.h>

int main() {
char name[] = "Hayasri";
char degree[] = "BE";
int result = strcmp(name,degree);
if (result==0){
    printf("same string :%s and %s\n",name);
}
    else
    {
    printf("different string:%s and  %s\n",name);
    }
    return 0;
}
//ouput
different string:Hayasri and  BE
